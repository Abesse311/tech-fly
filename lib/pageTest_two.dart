import 'package:flutter/material.dart'; 


class PagetestTwo extends StatelessWidget {
  const PagetestTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: Text("Page two"),),
    );
  }
}